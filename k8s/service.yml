apiVersion: v1
kind: Service
metadata:
  name: normal-website
  namespace: default
spec:
  selector:
    app: normal-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
